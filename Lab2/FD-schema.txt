departments(_name_, UNIQUE(abbreviation))

Course(_code_, cname, credits, capacity, dname)

Program(_pname_, pabbreviation)

Student(UNIQUE(StudLogin), _idnr_ pname, bname, sname)

unsure of name(UNIQUE((code,studlogin)), _(code, position)_, grade)

LimitedCourse i think(_code_, UNIQUE(clname), UNIQUE(position))

